---
phase: "01-debug-asignacion-clases"
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - "apps/api/prisma/schema.prisma"
  - "apps/api/src/routes/portal.routes.ts"
  - "apps/api/src/routes/reservaciones.routes.ts"
autonomous: true
requirements: [BUG-01]
user_setup: []
must_haves:
  truths:
    - "Dashboard puede asignar cliente a clase sin error 500"
    - "Portal puede reservar clase sin error 500"
    - "Causa raíz del error está identificada y fixeada"
  artifacts:
    - path: "apps/api/prisma/schema.prisma"
      provides: "PaymentMethod enum con MERCADO_PAGO agregado"
      contains: "MERCADO_PAGO"
    - path: "apps/api/src/routes/portal.routes.ts"
      provides: "Portal reservation endpoint sin errores de tipo"
      contains: "MERCADO_PAGO"
    - path: "apps/api/src/routes/reservaciones.routes.ts"
      provides: "Dashboard reservation endpoint con null checks"
      contains: "membership &&"
  key_links:
    - from: "apps/api/src/routes/portal.routes.ts"
      to: "apps/api/prisma/schema.prisma"
      via: "Prisma client genera tipos desde schema"
      pattern: "PaymentMethod"
    - from: "apps/api/src/routes/portal.routes.ts:910-925"
      to: "Payment table"
      via: "Prisma Payment.upsert con reservationId"
      pattern: "payment\\.upsert"
---

<objective>
Fix error 500 en asignación de clases agregando MERCADO_PAGO al enum PaymentMethod y corrigiendo null checks.
</objective>

<execution_context>
@/Users/jesseilescas/Documents/sarui/sarui-studio/.opencode/get-shit-done/workflows/execute-plan.md
@/Users/jesseilescas/Documents/sarui/sarui-studio/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/jesseilescas/Documents/sarui/sarui-studio/.planning/phases/01-debug-asignacion-clases/01-CONTEXT.md
@/Users/jesseilescas/Documents/sarui/sarui-studio/apps/api/prisma/schema.prisma
@/Users/jesseilescas/Documents/sarui/sarui-studio/apps/api/src/routes/portal.routes.ts
@/Users/jesseilescas/Documents/sarui/sarui-studio/apps/api/src/routes/reservaciones.routes.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Agregar MERCADO_PAGO al PaymentMethod enum en schema.prisma</name>
  <files>apps/api/prisma/schema.prisma</files>
  <read_first>apps/api/prisma/schema.prisma (líneas 107-112)</read_first>
  <action>
    Editar el enum PaymentMethod en apps/api/prisma/schema.prisma (líneas 107-112) para agregar MERCADO_PAGO:

    Cambiar:
    ```prisma
    enum PaymentMethod {
      CASH
      CARD
      TRANSFER
      OTHER
    }
    ```

    Por:
    ```prisma
    enum PaymentMethod {
      CASH
      CARD
      TRANSFER
      OTHER
      MERCADO_PAGO
    }
    ```

    Esto es requerido porque portal.routes.ts usa 'MERCADO_PAGO' as PaymentMethod en:
    - Línea 840: paymentMethod: 'MERCADO_PAGO' as PaymentMethod
    - Línea 850: method: 'MERCADO_PAGO' as PaymentMethod
    - Línea 915: method: 'CARD' (esto también debe cambiarse a MERCADO_PAGO, ver Task 2)

    Después de editar, ejecutar:
    ```bash
    cd apps/api && npx prisma db push --accept-data-loss
    ```
    Esto actualiza el schema en la base de datos.
  </action>
  <verify>
    <automated>grep -A 5 "enum PaymentMethod" apps/api/prisma/schema.prisma | grep -q "MERCADO_PAGO" && echo "PASS: MERCADO_PAGO agregado al enum" || echo "FAIL: MERCADO_PAGO no encontrado"</automated>
  </verify>
  <acceptance_criteria>
    - apps/api/prisma/schema.prisma contiene "MERCADO_PAGO" dentro del enum PaymentMethod
    - El enum tiene exactamente 5 valores: CASH, CARD, TRANSFER, OTHER, MERCADO_PAGO
    - Prisma schema compila sin errores (npx prisma validate exit 0)
  </acceptance_criteria>
  <done>PaymentMethod enum actualizado con MERCADO_PAGO, schema validado</done>
</task>

<task type="auto">
  <name>Task 2: Fix portal.routes.ts - corregir método de pago y null check</name>
  <files>apps/api/src/routes/portal.routes.ts</files>
  <read_first>apps/api/src/routes/portal.routes.ts (líneas 866-930)</read_first>
  <action>
    En apps/api/src/routes/portal.routes.ts, corregir los siguientes issues:

    1. Línea 914-915: Cambiar method: 'CARD' por method: 'MERCADO_PAGO' en el Payment.upsert:
       ```typescript
       // Antes (línea 915):
       method: 'CARD',

       // Después:
       method: 'MERCADO_PAGO',
       ```

    2. Línea 913: Cambiar amount para manejar null de transaction_amount:
       ```typescript
       // Antes (línea 913):
       amount: payment.transaction_amount ?? 0,

       // Después (usar valor de la clase como fallback):
       amount: payment.transaction_amount ?? clase.tipoActividad?.paquetes?.[0]?.price ?? 0,
       ```

    3. Línea 876: Validar que payment no sea null antes de usarlo:
       ```typescript
       // Después de línea 875 (const payment = await getPayment(paymentId);), agregar:
       if (!payment) {
         ApiError(res, 'PAYMENT_NOT_FOUND', 'No se pudo obtener información del pago', 400);
         return;
       }
       ```
  </action>
  <verify>
    <automated>grep -n "method: 'MERCADO_PAGO'" apps/api/src/routes/portal.routes.ts | grep -E "(914|915|916)" && grep -A 2 "const payment = await getPayment" apps/api/src/routes/portal.routes.ts | grep -q "if (!payment)" && echo "PASS: Fixes aplicados" || echo "FAIL: Revisar cambios"</automated>
  </verify>
  <acceptance_criteria>
    - Línea ~915: method es 'MERCADO_PAGO' (no 'CARD')
    - Línea ~913: amount maneja null de transaction_amount con fallback
    - Después de getPayment(): validación if (!payment) con ApiError correcto
    - No hay errores de TypeScript (npx tsc --noEmit exit 0 en apps/api)
  </acceptance_criteria>
  <done>Portal reservation endpoint maneja pagos MercadoPago correctamente con null checks</done>
</task>

<task type="auto">
  <name>Task 3: Fix reservaciones.routes.ts - agregar null check para membership</name>
  <files>apps/api/src/routes/reservaciones.routes.ts</files>
  <read_first>apps/api/src/routes/reservaciones.routes.ts (líneas 146-160)</read_first>
  <action>
    En apps/api/src/routes/reservaciones.routes.ts, corregir el null check para membership (D-03, S-01 del CONTEXT.md):

    Líneas 146-159, cambiar:
    ```typescript
    if (parseResult.data.membershipId) {
      const membership = await tx.membership.findUnique({
        where: { id: parseResult.data.membershipId },
      });
      if (membership && membership.sessionsRemaining > 0) {
        await tx.membership.update({
          where: { id: parseResult.data.membershipId },
          data: {
            sessionsUsed: { increment: 1 },
            sessionsRemaining: { decrement: 1 },
            status: membership.sessionsRemaining - 1 <= 0 ? 'EXHAUSTED' : 'ACTIVE',
          },
        });
      }
    }
    ```

    El código ya tiene el null check (`if (membership && ...)`), pero falta validar que la membresía pertenece al cliente. Agregar antes del update:

    ```typescript
    if (parseResult.data.membershipId) {
      const membership = await tx.membership.findUnique({
        where: { id: parseResult.data.membershipId },
      });
      // Validar que la membresía existe Y pertenece al cliente
      if (membership && membership.clientId === parseResult.data.clientId && membership.sessionsRemaining > 0) {
        await tx.membership.update({
          where: { id: parseResult.data.membershipId },
          data: {
            sessionsUsed: { increment: 1 },
            sessionsRemaining: { decrement: 1 },
            status: membership.sessionsRemaining - 1 <= 0 ? 'EXHAUSTED' : 'ACTIVE',
          },
        });
      } else if (membership && membership.clientId !== parseResult.data.clientId) {
        // La membresía no pertenece a este cliente - no decrementar
        // Solo crear la reservación sin tocar la membresía
      }
    }
    ```
  </action>
  <verify>
    <automated>grep -A 10 "if (parseResult.data.membershipId)" apps/api/src/routes/reservaciones.routes.ts | grep -q "membership.clientId === parseResult.data.clientId" && echo "PASS: Validación de cliente agregada" || echo "FAIL: Falta validación"</automated>
  </verify>
  <acceptance_criteria>
    - Código valida que membership.clientId === parseResult.data.clientId antes de decrementar sesiones
    - Si membresía no pertenece al cliente, no se decrementa (solo se crea reservación)
    - No hay errores de TypeScript (npx tsc --noEmit en apps/api)
    - Response envelope usa ApiError(res, 'INVALID_MEMBERSHIP', 'La membresía no pertenece a este cliente', 400) si aplica
  </acceptance_criteria>
  <done>Dashboard reservation endpoint tiene null checks y validación de pertenencia de membresía</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → API (dashboard) | Authenticated admin/instructor creates reservation |
| Client → API (portal) | Authenticated client creates reservation with payment |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | portal.routes.ts:525-533 | mitigate | Validar que clientId del token JWT coincide con la reservación (línea 897) |
| T-01-02 | Spoofing | portal.routes.ts:915 | mitigate | Usar MERCADO_PAGO correcto (ya no hay cast con valor inexistente) |
| T-01-03 | Information Disclosure | reservaciones.routes.ts:146-159 | mitigate | No exponer si membresía existe o no (usar código genérico) |
| T-01-04 | Denial of Service | portal.routes.ts:599 | accept | Rate limiting no implementado aún (fuera de scope) |
</threat_model>

<verification>
1. `cd apps/api && npx prisma validate` → exit 0
2. `cd apps/api && npx tsc --noEmit` → exit 0
3. `curl -X POST http://localhost:3000/api/v1/reservaciones -H "Authorization: Bearer $TOKEN" -d '{"clientId":"test","classId":"test"}'` → no 500
4. `grep "MERCADO_PAGO" apps/api/prisma/schema.prisma` → encontrado
</verification>

<success_criteria>
1. Schema prisma tiene MERCADO_PAGO en PaymentMethod enum
2. Portal routes usa method: 'MERCADO_PAGO' (no 'CARD')
3. Dashboard routes valida pertenencia de membresía antes de decrementar
4. No hay errores 500 en asignación de clases (dashboard y portal)
5. `npx prisma validate` y `npx tsc --noEmit` pasan en apps/api
</success_criteria>

<output>
After completion, create `.planning/phases/01-debug-asignacion-clases/01-01-SUMMARY.md`
</output>
