---
phase: "03-ayuda-dashboard"
plan: "02"
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - "apps/web/src/app/(dashboard)/ayuda/page.tsx"
  - "apps/web/src/app/(dashboard)/ayuda/layout.tsx"
  - "apps/web/src/components/ayuda/ManualAdmin.tsx"
  - "apps/web/src/components/ayuda/ManualCliente.tsx"
autonomous: true
requirements: [HELP-02, HELP-03]
user_setup: []
must_haves:
  truths:
    - "Help page loads at /ayuda with tabs for Admin and Client manuals"
    - "ManualAdmin component shows help sections for admin tasks"
    - "ManualCliente component shows help sections for client tasks"
    - "Page uses brand colors #254F40, #F6FFB5, #FDFFEC"
  artifacts:
    - path: "apps/web/src/app/(dashboard)/ayuda/page.tsx"
      provides: "Help page with Tabs navigation"
      contains: "Tabs"
    - path: "apps/web/src/components/ayuda/ManualAdmin.tsx"
      provides: "Admin manual content with Accordion sections"
      contains: "Accordion"
    - path: "apps/web/src/components/ayuda/ManualCliente.tsx"
      provides: "Client manual content with Accordion sections"
      contains: "Accordion"
  key_links:
    - from: "apps/web/src/app/(dashboard)/ayuda/page.tsx"
      to: "ManualAdmin.tsx, ManualCliente.tsx"
      via: "import and render inside TabsContent"
      pattern: "import.*ManualAdmin|import.*ManualCliente"
---

<objective>
Create the help page (/ayuda) with Tabs navigation and manual content components for admin and client.
</objective>

<purpose>
Implement the core help page (D-01, D-03, D-09, D-10) with real content, Tabs for navigation, and Accordion for collapsible sections.
</purpose>

<output>
- Help page at /ayuda with Tabs (Admin | Cliente)
- ManualAdmin.tsx with admin help content
- ManualCliente.tsx with client help content
- Components use Accordion, brand colors, framer-motion animations
</output>

<execution_context>
@/Users/jesseilescas/Documents/sarui/sarui-studio/.opencode/get-shit-done/workflows/execute-plan.md
@/Users/jesseilescas/Documents/sarui/sarui-studio/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/jesseilescas/Documents/sarui/sarui-studio/.planning/phases/03-ayuda-dashboard/03-CONTEXT.md
@/Users/jesseilescas/Documents/sarui/sarui-studio/.planning/codebase/CONVENTIONS.md
@/Users/jesseilescas/Documents/sarui/sarui-studio/.planning/codebase/STACK.md
@/Users/jesseilescas/Documents/sarui/sarui-studio/apps/web/src/app/(dashboard)/layout.tsx
@/Users/jesseilescas/Documents/sarui/sarui-studio/apps/web/src/lib/utils.ts
@/Users/jesseilescas/Documents/sarui/sarui-studio/apps/web/src/lib/animations.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ayuda page with Tabs and framer-motion animations</name>
  <files>apps/web/src/app/(dashboard)/ayuda/page.tsx, apps/web/src/app/(dashboard)/ayuda/layout.tsx</files>
  <read_first>apps/web/src/app/(dashboard)/layout.tsx (auth guard pattern), apps/web/src/lib/animations.ts (fadeIn variant), apps/web/src/components/ui/tabs.tsx (Tabs API)</read_first>
  <action>
    Create the help page at `apps/web/src/app/(dashboard)/ayuda/`.
    
    **layout.tsx** (if not auto-generated by Next.js):
    ```tsx
    "use client";
    import { motion, AnimatePresence } from "framer-motion";
    import { Sidebar } from "@/components/layout/Sidebar";
    import { Topbar } from "@/components/layout/Topbar";
    import { useUIStore } from "@/store/uiStore";
    import { cn } from "@/lib/utils";
    import { fadeIn } from "@/lib/animations";
    
    export default function AyudaLayout({ children }: { children: React.ReactNode }) {
      const sidebarOpen = useUIStore((s) => s.sidebarOpen);
      return (
        <div className="flex h-screen overflow-hidden bg-[#FDFFEC]">
          <Sidebar />
          <div className={cn("flex flex-col flex-1 overflow-hidden transition-all duration-300", sidebarOpen ? "md:ml-60" : "md:ml-16")}>
            <Topbar />
            <AnimatePresence mode="wait">
              <motion.main key="ayuda-content" className="flex-1 overflow-y-auto p-6" variants={fadeIn} initial="initial" animate="animate" exit="exit">
                {children}
              </motion.main>
            </AnimatePresence>
          </div>
        </div>
      );
    }
    ```
    
    **page.tsx** — Main help page with Tabs:
    ```tsx
    "use client";
    import { Tabs, TabsList, TabsTrigger, TabsContent } from "@/components/ui/tabs";
    import { ManualAdmin } from "@/components/ayuda/ManualAdmin";
    import { ManualCliente } from "@/components/ayuda/ManualCliente";
    import { HelpCircle } from "lucide-react";
    import { motion } from "framer-motion";
    
    export default function AyudaPage() {
      return (
        <div className="max-w-5xl mx-auto">
          <motion.div initial={{ opacity: 0, y: -12 }} animate={{ opacity: 1, y: 0 }} className="mb-8">
            <div className="flex items-center gap-3 mb-2">
              <HelpCircle className="w-8 h-8 text-[#254F40]" />
              <h1 className="text-3xl font-light text-[#254F40]">Centro de Ayuda</h1>
            </div>
            <p className="text-[#749390]">Guías y manuales para administrar tu studio de Pilates</p>
          </motion.div>
          
          <Tabs defaultValue="admin" className="w-full">
            <TabsList className="grid w-full max-w-md grid-cols-2 mb-6 bg-[#FDFFEC] border border-[#254F40]/10">
              <TabsTrigger value="admin" className="data-[state=active]:bg-[#254F40] data-[state=active]:text-white">
                Manual Admin
              </TabsTrigger>
              <TabsTrigger value="cliente" className="data-[state=active]:bg-[#254F40] data-[state=active]:text-white">
                Manual Cliente
              </TabsTrigger>
            </TabsList>
            
            <TabsContent value="admin">
              <ManualAdmin />
            </TabsContent>
            <TabsContent value="cliente">
              <ManualCliente />
            </TabsContent>
          </Tabs>
        </div>
      );
    }
    ```
    
    Per D-12, use framer-motion `motion.div` for the header animation.
    Per D-11, brand colors: `#254F40` (primary), `#FDFFEC` (background).
  </action>
  <verify>
    <automated>test -f apps/web/src/app/\(dashboard\)/ayuda/page.tsx && grep -q "Tabs" apps/web/src/app/\(dashboard\)/ayuda/page.tsx && echo "Page created"</automated>
  </verify>
  <acceptance_criteria>
    - page.tsx exists at apps/web/src/app/(dashboard)/ayuda/page.tsx
    - Imports Tabs, TabsList, TabsTrigger, TabsContent from "@/components/ui/tabs"
    - Imports ManualAdmin and ManualCliente components
    - Uses framer-motion motion.div for header animation
    - Brand color #254F40 used for heading and TabsTrigger active state
    - Background uses #FDFFEC
    - No TypeScript errors
  </acceptance_criteria>
  <done>Help page created with Tabs navigation for Admin and Client manuals</done>
</task>

<task type="auto">
  <name>Task 2: Create ManualAdmin component with Accordion sections</name>
  <files>apps/web/src/components/ayuda/ManualAdmin.tsx</files>
  <read_first>apps/web/src/components/ui/accordion.tsx (Accordion API), apps/web/src/components/ui/card.tsx (Card pattern for reference)</read_first>
  <action>
    Create `apps/web/src/components/ayuda/ManualAdmin.tsx` with real help content using Accordion (D-04, D-09).
    
    **Content sections (per D-09):** gestión de clientes, clases, instructores, membresías, paquetes, reservaciones.
    
    ```tsx
    "use client";
    import { Accordion, AccordionItem, AccordionTrigger, AccordionContent } from "@/components/ui/accordion";
    import { Card, CardHeader, CardTitle, CardContent } from "@/components/ui/card";
    import { Users, CalendarDays, UserCheck, CreditCard, Package, BookOpen } from "lucide-react";
    import { motion } from "framer-motion";
    
    const sections = [
      {
        icon: Users,
        title: "Gestión de Clientes",
        content: "Para registrar un nuevo cliente, navega a la sección 'Clientes' en el sidebar y haz click en 'Nuevo Cliente'. Completa el formulario con nombre, email, teléfono y PIN de 4 dígitos. El PIN permite al cliente acceder al portal y realizar reservaciones."
      },
      {
        icon: CalendarDays,
        title: "Gestión de Clases",
        content: "Ve a 'Clases' en el sidebar. Usa el calendario para seleccionar fecha y hora. Haz click en 'Nueva Clase', selecciona el tipo de actividad, instructor y capacidad. Puedes arrastrar en el calendario para seleccionar un rango de tiempo rápidamente."
      },
      {
        icon: UserCheck,
        title: "Gestión de Instructores",
        content: "En la sección 'Instructores' puedes dar de alta nuevos instructores. Completa su información y asígnales tipos de actividades que pueden impartir. El instructor aparecerá automáticamente en el dropdown al crear una clase."
      },
      {
        icon: CreditCard,
        title: "Gestión de Membresías y Paquetes",
        content: "Las membresías se gestionan en la sección 'Membresías'. Puedes crear diferentes planes (mensual, trimestral, anual) con número de clases incluidas. Los paquetes son opciones pre-pagadas de clases individuales. Al asignar una membresía a un cliente, el sistema decrementa las sesiones automáticamente."
      },
      {
        icon: BookOpen,
        title: "Gestión de Reservaciones",
        content: "Las reservaciones se listan en 'Reservaciones'. Para asignar un cliente a una clase, haz click en 'Asignar Cliente', busca al cliente y selecciona la clase. El portal también permite a los clientes reservar por su cuenta. Las reservaciones consumen sesiones de la membresía o paquete del cliente."
      }
    ];
    
    export function ManualAdmin() {
      return (
        <motion.div initial={{ opacity: 0 }} animate={{ opacity: 1 }} transition={{ delay: 0.2 }}>
          <Card className="border-[#254F40]/10">
            <CardHeader>
              <CardTitle className="text-[#254F40]">Manual de Administrador</CardTitle>
            </CardHeader>
            <CardContent>
              <Accordion type="single" collapsible className="w-full">
                {sections.map((section, idx) => (
                  <AccordionItem key={idx} value={`admin-${idx}`}>
                    <AccordionTrigger className="text-[#254F40] hover:text-[#254F40]/80">
                      <div className="flex items-center gap-2">
                        <section.icon className="w-5 h-5" />
                        {section.title}
                      </div>
                    </AccordionTrigger>
                    <AccordionContent className="text-[#749390] leading-relaxed">
                      {section.content}
                    </AccordionContent>
                  </AccordionItem>
                ))}
              </Accordion>
            </CardContent>
          </Card>
        </motion.div>
      );
    }
    ```
    
    Per D-05, use lucide-react icons. Per D-03, use shadcn/ui Card and Accordion.
  </action>
  <verify>
    <automated>test -f apps/web/src/components/ayuda/ManualAdmin.tsx && grep -q "Accordion" apps/web/src/components/ayuda/ManualAdmin.tsx && echo "ManualAdmin created"</automated>
  </verify>
  <acceptance_criteria>
    - ManualAdmin.tsx exists in apps/web/src/components/ayuda/
    - Uses Accordion, AccordionItem, AccordionTrigger, AccordionContent
    - Contains 5 sections: clientes, clases, instructores, membresías/paquetes, reservaciones
    - Each section has a lucide-react icon (Users, CalendarDays, UserCheck, CreditCard, BookOpen)
    - Content is in Spanish (per project convention)
    - Uses Card component for outer container
    - Brand color #254F40 for headings and icons
    - No TypeScript errors
  </acceptance_criteria>
  <done>ManualAdmin component created with 5 Accordion sections covering admin tasks</done>
</task>

<task type="auto">
  <name>Task 3: Create ManualCliente component with Accordion sections</name>
  <files>apps/web/src/components/ayuda/ManualCliente.tsx</files>
  <read_first>apps/web/src/components/ayuda/ManualAdmin.tsx (pattern reference), apps/web/src/components/ui/accordion.tsx</read_first>
  <action>
    Create `apps/web/src/components/ayuda/ManualCliente.tsx` with real help content for clients (D-10).
    
    **Content sections (per D-10):** cómo reservar clases, pagar con MercadoPago, cancelar reservación, ver membresía.
    
    ```tsx
    "use client";
    import { Accordion, AccordionItem, AccordionTrigger, AccordionContent } from "@/components/ui/accordion";
    import { Card, CardHeader, CardTitle, CardContent } from "@/components/ui/card";
    import { CalendarDays, CreditCard, XCircle, User } from "lucide-react";
    import { motion } from "framer-motion";
    
    const sections = [
      {
        icon: CalendarDays,
        title: "Cómo Reservar una Clase",
        content: "Inicia sesión en el portal con tu email y PIN. Navega a 'Clases Disponibles' y busca la clase que te interese. Haz click en 'Reservar' y confirma tu reservación. Si tienes una membresía o paquete activo, la clase se descontará automáticamente de tu saldo."
      },
      {
        icon: CreditCard,
        title: "Cómo Pagar con MercadoPago",
        content: "Al reservar una clase sin saldo disponible, el sistema te mostrará la opción de pagar con MercadoPago. Haz click en 'Pagar Ahora', se abrirá el checkout de MercadoPago. Completa el pago con tarjeta, transferencia o efectivo. Una vez confirmado, tu reservación quedará activa."
      },
      {
        icon: XCircle,
        title: "Cómo Cancelar una Reservación",
        content: "Ve a 'Mis Reservaciones' en el portal. Busca la clase que deseas cancelar y haz click en 'Cancelar'. Ten en cuenta que algunas membresías tienen políticas de cancelación (ej. con 24h de anticipación). Si cancelas a tiempo, la clase se reembolsará a tu saldo."
      },
      {
        icon: User,
        title: "Cómo Ver tu Membresía",
        content: "En 'Mi Perfil' puedes ver los detalles de tu membresía o paquete actual: fecha de vencimiento, clases restantes, y tipo de plan. También puedes renovar tu membresía desde esta sección si está por vencer."
      }
    ];
    
    export function ManualCliente() {
      return (
        <motion.div initial={{ opacity: 0 }} animate={{ opacity: 1 }} transition={{ delay: 0.2 }}>
          <Card className="border-[#254F40]/10">
            <CardHeader>
              <CardTitle className="text-[#254F40]">Manual de Cliente</CardTitle>
            </CardHeader>
            <CardContent>
              <Accordion type="single" collapsible className="w-full">
                {sections.map((section, idx) => (
                  <AccordionItem key={idx} value={`cliente-${idx}`}>
                    <AccordionTrigger className="text-[#254F40] hover:text-[#254F40]/80">
                      <div className="flex items-center gap-2">
                        <section.icon className="w-5 h-5" />
                        {section.title}
                      </div>
                    </AccordionTrigger>
                    <AccordionContent className="text-[#749390] leading-relaxed">
                      {section.content}
                    </AccordionContent>
                  </AccordionItem>
                ))}
              </Accordion>
            </CardContent>
          </Card>
        </motion.div>
      );
    }
    ```
  </action>
  <verify>
    <automated>test -f apps/web/src/components/ayuda/ManualCliente.tsx && grep -q "Accordion" apps/web/src/components/ayuda/ManualCliente.tsx && echo "ManualCliente created"</automated>
  </verify>
  <acceptance_criteria>
    - ManualCliente.tsx exists in apps/web/src/components/ayuda/
    - Uses Accordion components from shadcn/ui
    - Contains 4 sections: reservar clases, pagar con MercadoPago, cancelar reservación, ver membresía
    - Each section has a lucide-react icon (CalendarDays, CreditCard, XCircle, User)
    - Content is in Spanish
    - Uses Card component for outer container
    - Brand color #254F40 for headings and icons
    - No TypeScript errors
  </acceptance_criteria>
  <done>ManualCliente component created with 4 Accordion sections covering client tasks</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→Next.js | Help page renders static content (no user input processed) |
| client→Accordion/Tabs | Interactive UI components (client-side only) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-------------|-------------|-----------------|
| T-03-04 | S (Spoofing) | page.tsx | accept | Static content page; no auth challenges |
| T-03-05 | T (Tampering) | ManualAdmin.tsx | accept | Content is static; no user input |
| T-03-06 | E (Info Disclosure) | ManualAdmin.tsx | accept | No sensitive data in help content |
| T-03-07 | D (Denial) | page.tsx | accept | Standard Next.js page; no special DoS risk |
</threat_model>

<verification>
1. `apps/web/src/app/(dashboard)/ayuda/page.tsx` exists with Tabs navigation
2. `apps/web/src/components/ayuda/ManualAdmin.tsx` exists with Accordion sections
3. `apps/web/src/components/ayuda/ManualCliente.tsx` exists with Accordion sections
4. `npm run build` in apps/web passes
5. Page accessible at /ayuda route
</verification>

<success_criteria>
1. ✓ Help page loads at /ayuda with Tabs showing "Manual Admin" and "Manual Cliente"
2. ✓ ManualAdmin shows 5 help sections with Accordion (clientes, clases, instructores, membresías, reservaciones)
3. ✓ ManualCliente shows 4 help sections with Accordion (reservar, pagar, cancelar, perfil)
4. ✓ All content in Spanish
5. ✓ Brand colors #254F40, #FDFFEC used correctly
6. ✓ lucide-react icons used in each section
7. ✓ framer-motion animations on page load
</success_criteria>

<output>
After completion, create `.planning/phases/03-ayuda-dashboard/03-02-SUMMARY.md`
</output>
