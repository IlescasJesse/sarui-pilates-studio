---
phase: 23-nomina-personal
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/prisma/schema.prisma
autonomous: true
requirements: [SC-23-1, SC-23-2, SC-23-3, SC-23-4, SC-23-5]
must_haves:
  truths:
    - "Las tablas puestos, asistencia_personal, periodos_nomina, nomina_detalles existen en MySQL"
    - "StaffProfile tiene columnas puestoId, fechaIngreso, activo"
    - "El enum EstadoNomina existe con BORRADOR, APROBADO, PAGADO"
  artifacts:
    - path: "apps/api/prisma/schema.prisma"
      provides: "Modelos Puesto, AsistenciaPersonal, PeriodoNomina, NominaDetalle, enum EstadoNomina, StaffProfile extendido, relaciones User"
      contains: "model Puesto"
  key_links:
    - from: "NominaDetalle"
      to: "PeriodoNomina"
      via: "periodoId FK"
      pattern: "periodo\\s+PeriodoNomina @relation"
    - from: "AsistenciaPersonal"
      to: "User"
      via: "userId FK @@unique([userId, fecha])"
      pattern: "@@unique\\(\\[userId, fecha\\]\\)"
---

<objective>
Extender el schema Prisma con los 4 modelos nuevos de nómina (Puesto, AsistenciaPersonal, PeriodoNomina, NominaDetalle), el enum EstadoNomina, la extensión de StaffProfile y las relaciones inversas en User; luego aplicar la migración a MySQL.

Purpose: Toda la fase 23 (backend y frontend) depende de estas tablas. [BLOCKING] — ningún otro plan puede ejecutarse hasta que `prisma db push` complete y `prisma generate` regenere el cliente.
Output: Schema actualizado, DB migrada, cliente Prisma regenerado con los nuevos tipos.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/23-nomina-personal/23-CONTEXT.md
@.planning/phases/23-nomina-personal/23-RESEARCH.md

<interfaces>
<!-- Estado actual verificado del schema (extraído del código). El executor NO debe asumir; estos son los hechos. -->

Role enum (schema.prisma:19): ADMIN, INSTRUCTOR, RECEPCIONISTA, CLIENT
TipoCuenta enum (schema.prisma:26): ACTIVO, PASIVO, CAPITAL, INGRESO, COSTO, GASTO

StaffProfile actual (schema.prisma:548) tiene SOLO:
  id, userId @unique, firstName, lastName, phone?, avatarUrl?, createdAt, updatedAt
  user User @relation(fields:[userId], references:[id], onDelete: Cascade)
  @@index([userId]) @@map("staff_profiles")

User (schema.prisma:146) ya tiene relaciones: client?, instructor?, staffProfile?,
  refreshTokens[], gastosCreados Gasto[], ingresosCreados Ingreso[], movimientosInventario[]
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Agregar enum EstadoNomina y 4 modelos nuevos al schema</name>
  <files>apps/api/prisma/schema.prisma</files>
  <read_first>
    - apps/api/prisma/schema.prisma (leer modelos User, StaffProfile, Gasto, CuentaContable y los enums existentes para respetar el estilo de @@map, @@index y @db.Decimal(10,2))
  </read_first>
  <action>
    Agregar al final de la sección de personal/contabilidad del schema:

    1. enum EstadoNomina con valores BORRADOR, APROBADO, PAGADO (en ese orden).

    2. model Puesto: campos id String @id @default(cuid()), nombre String @unique, salarioSemanal Decimal @db.Decimal(10, 2), activo Boolean @default(true), createdAt DateTime @default(now()). Relación inversa staffProfiles StaffProfile[]. @@map("puestos").

    3. model AsistenciaPersonal: id String @id @default(cuid()), userId String, fecha DateTime @db.Date, presente Boolean @default(true), observaciones String?, creadoEn DateTime @default(now()). Relación user User @relation(fields:[userId], references:[id]). @@unique([userId, fecha]), @@index([userId]), @@index([fecha]), @@map("asistencia_personal").

    4. model PeriodoNomina: id String @id @default(cuid()), fechaInicio DateTime @db.Date, fechaFin DateTime @db.Date, estado EstadoNomina @default(BORRADOR), creadoPorId String, creadoEn DateTime @default(now()). Relaciones: creadoPor User @relation(fields:[creadoPorId], references:[id]), detalles NominaDetalle[]. @@index([estado]), @@map("periodos_nomina").

    5. model NominaDetalle: id String @id @default(cuid()), periodoId String, userId String, diasTrabajados Int, salarioBruto Decimal @db.Decimal(10, 2), deducciones Decimal @db.Decimal(10, 2) @default(0), netoAPagar Decimal @db.Decimal(10, 2), gastoId String? @unique. Relaciones: periodo PeriodoNomina @relation(fields:[periodoId], references:[id]), user User @relation(fields:[userId], references:[id]). @@unique([periodoId, userId]), @@map("nomina_detalles").

    NO crear relación desde NominaDetalle hacia Gasto (gastoId es solo un puntero suelto @unique, no FK relacional — Gasto no necesita relación inversa).
  </action>
  <verify>
    <automated>grep -c "model Puesto\|model AsistenciaPersonal\|model PeriodoNomina\|model NominaDetalle\|enum EstadoNomina" apps/api/prisma/schema.prisma</automated>
  </verify>
  <acceptance_criteria>
    - schema.prisma contiene `enum EstadoNomina {` con BORRADOR, APROBADO, PAGADO
    - schema.prisma contiene `model Puesto {`, `model AsistenciaPersonal {`, `model PeriodoNomina {`, `model NominaDetalle {`
    - AsistenciaPersonal contiene `@@unique([userId, fecha])`
    - NominaDetalle contiene `@@unique([periodoId, userId])` y `gastoId String? @unique`
    - salarioSemanal, salarioBruto, deducciones, netoAPagar usan `@db.Decimal(10, 2)`
  </acceptance_criteria>
  <done>Los 4 modelos, el enum y todos los índices/constraints están en schema.prisma.</done>
</task>

<task type="auto">
  <name>Task 2: Extender StaffProfile y agregar relaciones inversas en User</name>
  <files>apps/api/prisma/schema.prisma</files>
  <read_first>
    - apps/api/prisma/schema.prisma (model StaffProfile línea ~548, model User línea ~146)
  </read_first>
  <action>
    En model StaffProfile agregar tres campos nuevos: puestoId String?, fechaIngreso DateTime? @db.Date, activo Boolean @default(true). Agregar dos relaciones: puesto Puesto? @relation(fields:[puestoId], references:[id]) y nominaDetalles NominaDetalle[]. Conservar todos los campos existentes (id, userId, firstName, lastName, phone, avatarUrl, createdAt, updatedAt, user, @@index([userId]), @@map).

    En model User agregar tres relaciones inversas nuevas a la lista existente: asistencias AsistenciaPersonal[], periodosNomina PeriodoNomina[], nominaDetalles NominaDetalle[]. NO eliminar las relaciones existentes (gastosCreados, ingresosCreados, etc.).
  </action>
  <verify>
    <automated>grep -c "puestoId String?\|fechaIngreso DateTime? @db.Date\|asistencias AsistenciaPersonal\[\]\|periodosNomina PeriodoNomina\[\]" apps/api/prisma/schema.prisma</automated>
  </verify>
  <acceptance_criteria>
    - StaffProfile contiene `puestoId String?`, `fechaIngreso DateTime? @db.Date`, `activo Boolean @default(true)`
    - StaffProfile contiene `puesto Puesto? @relation(fields: [puestoId], references: [id])`
    - User contiene `asistencias AsistenciaPersonal[]`, `periodosNomina PeriodoNomina[]`, `nominaDetalles NominaDetalle[]`
    - StaffProfile conserva firstName, lastName, @@map("staff_profiles")
  </acceptance_criteria>
  <done>StaffProfile extendido y User con relaciones inversas; el schema es consistente referencialmente.</done>
</task>

<task type="auto">
  <name>Task 3: [BLOCKING] Aplicar migración y regenerar cliente Prisma</name>
  <files>apps/api/prisma/schema.prisma</files>
  <read_first>
    - apps/api/prisma/schema.prisma (verificar que Tasks 1-2 quedaron bien antes de migrar)
  </read_first>
  <action>
    Desde apps/api ejecutar `npx prisma db push` para aplicar el schema a MySQL (el proyecto usa db push según convención, no migrate dev). Luego `npx prisma generate` para regenerar el cliente con los tipos Puesto, AsistenciaPersonal, PeriodoNomina, NominaDetalle, EstadoNomina. Si MySQL no está corriendo localmente, registrar el fallo en el SUMMARY como blocker A1 del RESEARCH (la migración requerirá ejecución manual de Jesse) pero igualmente correr `npx prisma generate` para que el cliente tenga los tipos y el backend compile.
  </action>
  <verify>
    <automated>cd apps/api && npx prisma validate</automated>
  </verify>
  <acceptance_criteria>
    - `npx prisma validate` retorna "The schema at prisma/schema.prisma is valid"
    - `npx prisma generate` completa sin error y node_modules/.prisma/client tiene los nuevos tipos
    - Si db push corre: las tablas puestos, asistencia_personal, periodos_nomina, nomina_detalles existen en la BD
    - Si MySQL no disponible: SUMMARY documenta el blocker y prisma generate sí completó
  </acceptance_criteria>
  <done>Schema válido, cliente Prisma regenerado, migración aplicada (o blocker documentado si MySQL no corre local).</done>
</task>

</tasks>

<verification>
- `cd apps/api && npx prisma validate` pasa
- `cd apps/api && npx tsc --noEmit` no reporta errores de tipos faltantes de los nuevos modelos
</verification>

<success_criteria>
Schema Prisma extendido con 4 modelos + enum + StaffProfile + relaciones User. Cliente regenerado. Esta es la base [BLOCKING] de toda la fase.
</success_criteria>

<output>
Create `.planning/phases/23-nomina-personal/23-01-SUMMARY.md` when done
</output>
