---
phase: 23-nomina-personal
plan: 02
type: execute
wave: 2
depends_on: [23-01]
files_modified:
  - apps/api/src/routes/personal.routes.ts
  - apps/api/src/routes/index.ts
autonomous: true
requirements: [SC-23-1, SC-23-2]
must_haves:
  truths:
    - "ADMIN puede crear, listar, editar y desactivar puestos vía API"
    - "ADMIN puede crear y listar perfiles de personal con puesto, fechaIngreso y activo"
    - "Las rutas /personal están montadas en el router principal"
  artifacts:
    - path: "apps/api/src/routes/personal.routes.ts"
      provides: "CRUD de puestos y personal (StaffProfile) protegido por requireRole ADMIN"
      exports: ["default router"]
    - path: "apps/api/src/routes/index.ts"
      provides: "Montaje de personalRoutes en /personal"
      contains: "personalRoutes"
  key_links:
    - from: "apps/api/src/routes/index.ts"
      to: "personal.routes.ts"
      via: "router.use('/personal', personalRoutes)"
      pattern: "router.use\\('/personal'"
---

<objective>
Crear el archivo de rutas personal.routes.ts con el CRUD de Puestos y el CRUD de Personal (StaffProfile extendido), y montarlo en el router principal. Incluye creación inline de puesto durante la asignación de personal.

Purpose: Cubre SC-23-1 (CRUD puestos) y SC-23-2 (StaffProfile con puestoId, fechaIngreso, activo). Es la base de rutas sobre la que los planes 03 y 04 agregan asistencia y nómina al MISMO archivo.
Output: Endpoints /personal/puestos y /personal/staff funcionando, ruta montada.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/23-nomina-personal/23-RESEARCH.md
@apps/api/src/routes/inventario.routes.ts

<interfaces>
<!-- Patrones verificados del proyecto -->
Response utils (apps/api/src/utils/response.ts):
  ApiSuccess(res, data, statusCode?)  // statusCode opcional, default 200
  ApiError(res, codeString, message, statusCode)

Middleware:
  authMiddleware (apps/api/src/middlewares/auth.middleware.ts) — pone req.user = { id, role, ... }
  requireRole('ADMIN', ...) (apps/api/src/middlewares/role.middleware.ts)

Patrón de archivo (inventario.routes.ts):
  router.use(authMiddleware); router.use(requireRole('ADMIN'));  // protege TODO el archivo
  Validación: zodSchema.safeParse(req.body); if(!parse.success) ApiError(res,'VALIDATION_ERROR',msg,400)

prisma importado desde '../config/database'
StaffProfile FK: userId @unique → un User tiene a lo más un StaffProfile (onDelete Cascade)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Crear personal.routes.ts con CRUD de Puestos</name>
  <files>apps/api/src/routes/personal.routes.ts</files>
  <read_first>
    - apps/api/src/routes/inventario.routes.ts (replicar header: imports, router.use(authMiddleware), router.use(requireRole('ADMIN')), patrón safeParse + ApiError)
    - apps/api/src/utils/response.ts (firmas exactas de ApiSuccess/ApiError)
  </read_first>
  <action>
    Crear el archivo con los imports estándar (Router, Request/Response/NextFunction, z, authMiddleware, requireRole, prisma, ApiSuccess, ApiError). Aplicar al inicio `router.use(authMiddleware)`. NO aplicar requireRole('ADMIN') global al archivo todavía — los planes 03 (asistencia empleado) y 04 (mi-historial) agregarán rutas no-admin; usar requireRole('ADMIN') por-ruta en lugar de global.

    Definir puestoSchema con z: nombre z.string().trim().min(1), salarioSemanal z.coerce.number().positive(), activo z.boolean().optional().

    Rutas de puestos (todas con requireRole('ADMIN') por-ruta):
    - GET /puestos: prisma.puesto.findMany ordenado por nombre asc. ApiSuccess(res, items).
    - POST /puestos: safeParse puestoSchema; crear puesto. ApiSuccess(res, puesto, 201). Si nombre duplicado (P2002) → ApiError(res,'DUPLICATE','Ya existe un puesto con ese nombre',409).
    - PATCH /puestos/:id: safeParse parcial (puestoSchema.partial()); prisma.puesto.update. ApiError 404 si no existe.
    - DELETE /puestos/:id: verificar que ningún StaffProfile tenga ese puestoId (prisma.staffProfile.count where puestoId); si count>0 → ApiError(res,'IN_USE','No se puede eliminar: hay empleados asignados',409). Si no, prisma.puesto.delete. ApiSuccess(res,{ok:true}).

    Exportar `export default router;`.
  </action>
  <verify>
    <automated>cd apps/api && npx tsc --noEmit 2>&1 | grep -c "personal.routes" || true</automated>
  </verify>
  <acceptance_criteria>
    - personal.routes.ts existe y exporta default router
    - Contiene rutas GET /puestos, POST /puestos, PATCH /puestos/:id, DELETE /puestos/:id
    - DELETE /puestos/:id retorna 409 si hay StaffProfile con ese puestoId
    - POST /puestos retorna 409 ante nombre duplicado (P2002)
    - `npx tsc --noEmit` no reporta errores en personal.routes.ts
  </acceptance_criteria>
  <done>CRUD de puestos completo, validado con Zod, protegido con requireRole('ADMIN').</done>
</task>

<task type="auto">
  <name>Task 2: Agregar CRUD de Personal (StaffProfile) con creación inline de puesto</name>
  <files>apps/api/src/routes/personal.routes.ts</files>
  <read_first>
    - apps/api/src/routes/personal.routes.ts (archivo creado en Task 1)
    - apps/api/prisma/schema.prisma (StaffProfile extendido: userId @unique, puestoId, fechaIngreso, activo)
  </read_first>
  <action>
    Agregar al mismo archivo (todas requireRole('ADMIN') por-ruta):

    staffSchema con z: userId z.string().min(1), firstName z.string().trim().min(1), lastName z.string().trim().min(1), phone z.string().optional(), puestoId z.string().optional(), fechaIngreso z.string().regex(/^\d{4}-\d{2}-\d{2}$/).optional(), activo z.boolean().optional(), nuevoPuesto z.object({ nombre: z.string().min(1), salarioSemanal: z.coerce.number().positive() }).optional().

    - GET /staff: prisma.staffProfile.findMany include { user: { select: { id, email, role } }, puesto: true } ordenado por firstName asc. ApiSuccess(res, items).
    - POST /staff: safeParse staffSchema. Si viene nuevoPuesto y no puestoId: dentro de prisma.$transaction crear el puesto primero y usar su id como puestoId. Crear staffProfile con fechaIngreso convertida a new Date(fechaIngreso) si presente. Manejar P2002 sobre userId @unique → ApiError(res,'DUPLICATE','Ese usuario ya tiene perfil de personal',409). ApiSuccess(res, staff, 201).
    - PATCH /staff/:id: safeParse staffSchema.partial(). Actualizar campos presentes (firstName, lastName, phone, puestoId, fechaIngreso→Date, activo). ApiError 404 si no existe. ApiSuccess(res, updated).
    - DELETE /staff/:id: prisma.staffProfile.delete. ApiSuccess(res,{ok:true}).
  </action>
  <verify>
    <automated>cd apps/api && npx tsc --noEmit 2>&1 | grep -c "personal.routes" || true</automated>
  </verify>
  <acceptance_criteria>
    - Contiene rutas GET /staff, POST /staff, PATCH /staff/:id, DELETE /staff/:id
    - POST /staff crea puesto inline dentro de $transaction cuando se envía nuevoPuesto sin puestoId
    - POST /staff retorna 409 si el userId ya tiene StaffProfile (P2002)
    - GET /staff incluye user (id,email,role) y puesto en cada registro
    - fechaIngreso se persiste como Date, no como string
  </acceptance_criteria>
  <done>CRUD de personal completo con creación inline de puesto en transacción.</done>
</task>

<task type="auto">
  <name>Task 3: Montar personalRoutes en el router principal</name>
  <files>apps/api/src/routes/index.ts</files>
  <read_first>
    - apps/api/src/routes/index.ts (ver el bloque de imports y el bloque de router.use protegidos)
  </read_first>
  <action>
    Agregar `import personalRoutes from './personal.routes';` junto a los demás imports. En la sección "Protected routes" agregar `router.use('/personal', personalRoutes);` (después de inventarioRoutes o finanzasRoutes). NO tocar las rutas públicas ni reordenar las existentes.
  </action>
  <verify>
    <automated>grep -c "router.use('/personal', personalRoutes)" apps/api/src/routes/index.ts</automated>
  </verify>
  <acceptance_criteria>
    - index.ts contiene `import personalRoutes from './personal.routes';`
    - index.ts contiene `router.use('/personal', personalRoutes);`
    - `cd apps/api && npx tsc --noEmit` no reporta errores nuevos
  </acceptance_criteria>
  <done>Ruta /personal montada; los endpoints de puestos y staff son alcanzables bajo /api/v1/personal.</done>
</task>

</tasks>

<verification>
- `cd apps/api && npx tsc --noEmit` compila sin errores
- Endpoints GET/POST/PATCH/DELETE /personal/puestos y /personal/staff existen
</verification>

<success_criteria>
SC-23-1 y SC-23-2 cubiertos: CRUD de puestos desde admin y StaffProfile extendido gestionable vía API, con creación inline de puesto.
</success_criteria>

<output>
Create `.planning/phases/23-nomina-personal/23-02-SUMMARY.md` when done
</output>
