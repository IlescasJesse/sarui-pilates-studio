---
phase: 23-nomina-personal
plan: 03
type: execute
wave: 3
depends_on: [23-01, 23-02]
files_modified:
  - apps/api/src/routes/asistencia.routes.ts
  - apps/api/src/routes/index.ts
autonomous: true
requirements: [SC-23-3]
must_haves:
  truths:
    - "Un empleado autenticado puede marcar SU asistencia solo para el día de hoy (fecha servidor)"
    - "Marcar dos veces el mismo día no duplica registros (upsert por userId+fecha)"
    - "ADMIN puede leer la asistencia de una semana y editar cualquier fecha retroactivamente"
  artifacts:
    - path: "apps/api/src/routes/asistencia.routes.ts"
      provides: "Autoregistro de asistencia (empleado) y lectura/edición admin"
      exports: ["default router"]
  key_links:
    - from: "asistencia.routes.ts POST /hoy"
      to: "prisma.asistenciaPersonal.upsert"
      via: "userId del token + fecha servidor"
      pattern: "userId_fecha"
---

<objective>
Crear asistencia.routes.ts montado en /personal/asistencia: autoregistro del empleado (solo HOY, fecha servidor) idempotente vía upsert, y endpoints admin para leer la asistencia semanal y editar retroactivamente cualquier fecha.

Purpose: Cubre SC-23-3 (AsistenciaPersonal con upsert por userId+fecha). Archivo SEPARADO de personal.routes.ts. Depende del plan 02 porque ambos editan index.ts (montaje aditivo secuencial para evitar conflicto).
Output: Endpoints de asistencia funcionando con la restricción temporal del lado servidor.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/23-nomina-personal/23-RESEARCH.md

<interfaces>
authMiddleware pone req.user = { id, role }. requireRole('ADMIN', 'INSTRUCTOR', 'RECEPCIONISTA').
ApiSuccess(res, data, statusCode?), ApiError(res, code, message, statusCode).
prisma desde '../config/database'.

AsistenciaPersonal (schema): id, userId, fecha @db.Date, presente Boolean @default(true),
  observaciones String?, @@unique([userId, fecha]) → clave compuesta Prisma: userId_fecha

PITFALL CRÍTICO (RESEARCH §Pitfall 1): el endpoint del empleado NUNCA acepta userId ni fecha
del body. fecha = new Date() del servidor con setHours(0,0,0,0). userId = req.user!.id.
Admin usa endpoint distinto que SÍ acepta fecha y userId en el body.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Crear asistencia.routes.ts con autoregistro de empleado (solo HOY)</name>
  <files>apps/api/src/routes/asistencia.routes.ts</files>
  <read_first>
    - apps/api/src/routes/inventario.routes.ts (header e imports estándar)
    - apps/api/prisma/schema.prisma (model AsistenciaPersonal — clave userId_fecha)
  </read_first>
  <action>
    Crear el archivo con imports estándar. router.use(authMiddleware) al inicio.

    autoregistroSchema con z: presente z.boolean().optional(), observaciones z.string().optional(). NO incluir userId ni fecha en este schema.

    - POST /hoy (cualquier rol staff — requireRole('ADMIN','INSTRUCTOR','RECEPCIONISTA')): derivar `const hoy = new Date(); hoy.setHours(0,0,0,0);`. prisma.asistenciaPersonal.upsert where { userId_fecha: { userId: req.user!.id, fecha: hoy } }, create { userId: req.user!.id, fecha: hoy, presente: presente ?? true, observaciones }, update { presente: presente ?? true, observaciones }. ApiSuccess(res, registro). El fecha del body, si llega, se IGNORA.
    - GET /mi-semana?inicio=YYYY-MM-DD (mismo requireRole staff): leer asistencias de req.user!.id entre inicio e inicio+6 días. ApiSuccess(res, items).
  </action>
  <verify>
    <automated>grep -c "userId_fecha\|hoy.setHours(0, 0, 0, 0)\|hoy.setHours(0,0,0,0)" apps/api/src/routes/asistencia.routes.ts</automated>
  </verify>
  <acceptance_criteria>
    - POST /hoy usa `new Date()` del servidor y `setHours(0,0,0,0)`; NO lee fecha del body
    - POST /hoy usa `req.user!.id`; NO lee userId del body
    - Usa prisma.asistenciaPersonal.upsert con clave userId_fecha (idempotente)
    - autoregistroSchema no contiene los campos userId ni fecha
    - `cd apps/api && npx tsc --noEmit` sin errores en asistencia.routes.ts
  </acceptance_criteria>
  <done>Autoregistro idempotente con restricción temporal server-side; doble POST mismo día no duplica.</done>
</task>

<task type="auto">
  <name>Task 2: Endpoints admin de lectura semanal y edición retroactiva</name>
  <files>apps/api/src/routes/asistencia.routes.ts</files>
  <read_first>
    - apps/api/src/routes/asistencia.routes.ts (archivo de Task 1)
  </read_first>
  <action>
    Agregar al mismo archivo:

    adminMarcarSchema con z: userId z.string().min(1), fecha z.string().regex(/^\d{4}-\d{2}-\d{2}$/), presente z.boolean(), observaciones z.string().optional().

    - GET /admin?inicio=YYYY-MM-DD (requireRole('ADMIN','INSTRUCTOR')): calcular rango [inicio, inicio+6d]. Devolver la lista de StaffProfile activos (prisma.staffProfile.findMany where activo:true include user) junto con sus asistencias en ese rango (prisma.asistenciaPersonal.findMany where fecha between). Estructurar respuesta como { staff: [...], asistencias: [...] } para que el frontend arme la grilla. ApiSuccess(res, payload). NOTA: para INSTRUCTOR no exponer salarioSemanal — no incluir puesto.salarioSemanal en este endpoint (solo nombre de puesto).
    - PATCH /admin (requireRole('ADMIN')): safeParse adminMarcarSchema. fecha = new Date(fecha) con horas a 0. upsert asistenciaPersonal where userId_fecha { userId, fecha }, create/update con presente y observaciones. Esto permite edición retroactiva de cualquier fecha. ApiSuccess(res, registro).
  </action>
  <verify>
    <automated>grep -c "GET\|/admin\|requireRole" apps/api/src/routes/asistencia.routes.ts</automated>
  </verify>
  <acceptance_criteria>
    - GET /admin retorna { staff, asistencias } para la semana solicitada
    - GET /admin NO incluye salarioSemanal en la respuesta (protección a INSTRUCTOR)
    - PATCH /admin requiere rol ADMIN y permite fecha arbitraria del body (edición retroactiva)
    - PATCH /admin usa upsert con clave userId_fecha
  </acceptance_criteria>
  <done>Admin puede leer asistencia semanal y corregir cualquier fecha; INSTRUCTOR lee sin ver salarios.</done>
</task>

<task type="auto">
  <name>Task 3: Montar asistenciaRoutes bajo /personal/asistencia</name>
  <files>apps/api/src/routes/index.ts</files>
  <read_first>
    - apps/api/src/routes/index.ts (ya contiene el montaje de /personal del plan 02)
  </read_first>
  <action>
    Agregar `import asistenciaRoutes from './asistencia.routes';`. En "Protected routes" agregar `router.use('/personal/asistencia', asistenciaRoutes);` ANTES de la línea `router.use('/personal', personalRoutes);` para que Express resuelva el path más específico primero. NO eliminar imports ni rutas existentes.
  </action>
  <verify>
    <automated>grep -c "router.use('/personal/asistencia', asistenciaRoutes)" apps/api/src/routes/index.ts</automated>
  </verify>
  <acceptance_criteria>
    - index.ts contiene `import asistenciaRoutes from './asistencia.routes';`
    - index.ts contiene `router.use('/personal/asistencia', asistenciaRoutes);` registrada antes de `/personal`
    - `cd apps/api && npx tsc --noEmit` sin errores nuevos
  </acceptance_criteria>
  <done>Endpoints de asistencia alcanzables bajo /api/v1/personal/asistencia.</done>
</task>

</tasks>

<verification>
- `cd apps/api && npx tsc --noEmit` compila
- POST /personal/asistencia/hoy ignora fecha/userId del body
</verification>

<success_criteria>
SC-23-3 cubierto: AsistenciaPersonal con upsert idempotente, autoregistro restringido a HOY server-side, edición admin retroactiva.
</success_criteria>

<output>
Create `.planning/phases/23-nomina-personal/23-03-SUMMARY.md` when done
</output>
