---
phase: 23-nomina-personal
plan: 04
type: execute
wave: 4
depends_on: [23-02, 23-03]
files_modified:
  - apps/api/src/services/nomina.service.ts
  - apps/api/src/routes/nomina.routes.ts
  - apps/api/src/routes/index.ts
autonomous: true
requirements: [SC-23-4, SC-23-5, SC-23-6]
must_haves:
  truths:
    - "ADMIN crea un período de nómina y el servidor calcula diasTrabajados y netoAPagar por empleado"
    - "Aprobar un período en BORRADOR crea un Gasto por empleado en cuenta 602 dentro de una $transaction"
    - "Aprobar un período que no está en BORRADOR retorna 409 (sin gastos duplicados)"
    - "ADMIN puede marcar un período APROBADO como PAGADO"
  artifacts:
    - path: "apps/api/src/services/nomina.service.ts"
      provides: "autoCreateGasto + calcularDetallesPeriodo"
      exports: ["autoCreateGasto", "calcularDetallesPeriodo"]
    - path: "apps/api/src/routes/nomina.routes.ts"
      provides: "CRUD períodos, recalcular, aprobar, marcar pagado, mi-historial"
      exports: ["default router"]
  key_links:
    - from: "nomina.routes.ts POST /periodos/:id/aprobar"
      to: "nomina.service.autoCreateGasto (cuenta 602)"
      via: "prisma.$transaction"
      pattern: "cuentaCodigo: '602'|cuentaCodigo:.?'602'"
    - from: "NominaDetalle.gastoId"
      to: "Gasto creado"
      via: "update dentro de la misma transacción"
      pattern: "gastoId"
---

<objective>
Implementar el corazón de la fase: el servicio nomina.service.ts (autoCreateGasto + cálculo de detalles) y nomina.routes.ts (crear período con cálculo server-side, recalcular, aprobar con efecto contable atómico en cuenta 602, marcar pagado, e historial del empleado).

Purpose: Cubre SC-23-4 (estados BORRADOR/APROBADO/PAGADO), SC-23-5 (NominaDetalle calculado) y SC-23-6 (aprobar → Gasto cuenta 602 en $transaction). Depende de los planes 02 y 03 porque comparte index.ts y necesita StaffProfile activo + asistencias.
Output: Endpoints de nómina completos con integración contable.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/23-nomina-personal/23-RESEARCH.md
@apps/api/src/services/membership.service.ts

<interfaces>
<!-- Hechos verificados -->
CUENTA CONTABLE: usar '602' / 'Sueldos y salarios' tipo GASTO. NO usar '501' (501 = Honorarios
instructores, COSTO). Confirmado en seed-cuentas.ts. CONTEXT.md tiene un error tipográfico.

Gasto (schema.prisma:588) campos: id, cuentaContableId, concepto, monto Decimal, fecha,
  comprobante?, notas?, creadoPorId. NO tiene campo 'origen'. autoCreateGasto NO debe pasar origen.

Patrón autoCreateIngreso (membership.service.ts:137): hace tx.cuentaContable.upsert por codigo,
  luego tx.gasto.create. Replicar con tipo 'GASTO' y devolver gasto.id.

Fórmula nómina (CONTEXT.md): netoAPagar = (salarioSemanal / 7) * diasTrabajados.
  diasTrabajados = count AsistenciaPersonal where presente=true AND fecha entre [inicio, fin].
  deducciones default 0; admin puede ajustar deducciones y netoAPagar manualmente antes de aprobar.

PITFALLS: validar estado==='BORRADOR' antes de aprobar (409 si no). staffProfile puede ser null →
  fallback a user.email para el concepto. Recalcular desde la tabla, no fiarse de un valor viejo.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: nomina.service.ts — autoCreateGasto y calcularDetallesPeriodo</name>
  <files>apps/api/src/services/nomina.service.ts</files>
  <read_first>
    - apps/api/src/services/membership.service.ts (firma exacta de autoCreateIngreso, patrón upsert cuenta + create)
    - apps/api/prisma/schema.prisma (Gasto sin campo origen; NominaDetalle, PeriodoNomina)
  </read_first>
  <behavior>
    - autoCreateGasto(tx, params) hace upsert de CuentaContable por codigo '602' (tipo GASTO) y crea un Gasto con monto/concepto/fecha/creadoPorId; devuelve el gastoId (string).
    - autoCreateGasto NO pasa el campo origen (Gasto no lo tiene).
    - calcularDetallesPeriodo(tx, periodoId, inicio, fin) recorre StaffProfile activos con puestoId, cuenta asistencias presente=true en rango, computa salarioBruto = salarioSemanal, netoAPagar = round((salarioSemanal/7)*dias, 2), deducciones=0, y upserta NominaDetalle por (periodoId, userId).
    - Si un empleado no tiene asistencias, dias=0 → netoAPagar=0 (no se omite la fila).
  </behavior>
  <action>
    Crear nomina.service.ts importando Prisma de '@prisma/client'.

    Exportar `autoCreateGasto(tx: Prisma.TransactionClient, params: { monto: number; concepto: string; fecha: Date; cuentaCodigo: string; cuentaNombre: string; creadoPorId: string }): Promise<string>`. Hacer tx.cuentaContable.upsert where {codigo: cuentaCodigo}, create {codigo, nombre: cuentaNombre, tipo:'GASTO'}, update {}. Luego tx.gasto.create data { cuentaContableId: cuenta.id, concepto, monto, fecha, creadoPorId }. Devolver gasto.id. NO incluir origen.

    Exportar `calcularDetallesPeriodo(tx: Prisma.TransactionClient, periodoId: string, inicio: Date, fin: Date): Promise<void>`. Obtener tx.staffProfile.findMany where { activo: true, puestoId: { not: null } } include { puesto: true }. Para cada staff: dias = await tx.asistenciaPersonal.count where { userId: staff.userId, presente: true, fecha: { gte: inicio, lte: fin } }. bruto = Number(staff.puesto.salarioSemanal). neto = Math.round((bruto/7)*dias*100)/100. Hacer tx.nominaDetalle.upsert where { periodoId_userId: { periodoId, userId: staff.userId } } create { periodoId, userId, diasTrabajados: dias, salarioBruto: bruto, deducciones: 0, netoAPagar: neto } update { diasTrabajados: dias, salarioBruto: bruto, netoAPagar: neto }. (En recálculo conservar deducciones existentes: en update NO sobrescribir deducciones; restar deducciones del neto en update — usar neto - Number(existing.deducciones) si aplica; para simplicidad de v1, en update recomputar netoAPagar = neto y dejar deducciones intactas.)
  </action>
  <verify>
    <automated>grep -c "cuentaCodigo\|'602'\|tipo: 'GASTO'\|calcularDetallesPeriodo\|autoCreateGasto" apps/api/src/services/nomina.service.ts</automated>
  </verify>
  <acceptance_criteria>
    - nomina.service.ts exporta autoCreateGasto y calcularDetallesPeriodo
    - autoCreateGasto hace upsert de cuenta con tipo 'GASTO' y NO pasa el campo origen al crear Gasto
    - calcularDetallesPeriodo cuenta solo asistencias con presente:true en el rango
    - netoAPagar = round((salarioSemanal/7)*diasTrabajados, 2)
    - `cd apps/api && npx tsc --noEmit` sin errores en nomina.service.ts
  </acceptance_criteria>
  <done>Servicio con cálculo correcto y creación de Gasto en cuenta 602 lista para usarse en la transacción de aprobación.</done>
</task>

<task type="auto">
  <name>Task 2: nomina.routes.ts — crear, recalcular, ajustar detalles, mi-historial</name>
  <files>apps/api/src/routes/nomina.routes.ts</files>
  <read_first>
    - apps/api/src/routes/inventario.routes.ts (header, safeParse, ApiError)
    - apps/api/src/services/nomina.service.ts (Task 1)
    - apps/api/prisma/schema.prisma (PeriodoNomina, NominaDetalle, EstadoNomina)
  </read_first>
  <action>
    Crear nomina.routes.ts. router.use(authMiddleware). Importar calcularDetallesPeriodo y autoCreateGasto del service.

    periodoSchema: fechaInicio z.string().regex(/^\d{4}-\d{2}-\d{2}$/), fechaFin igual.
    ajusteSchema: deducciones z.coerce.number().min(0), netoAPagar z.coerce.number().min(0).

    - GET /periodos (requireRole('ADMIN')): findMany include { detalles: { include: { user: { include: { staffProfile: { include: { puesto: true } } } } } } } orderBy creadoEn desc. ApiSuccess.
    - POST /periodos (ADMIN): safeParse periodoSchema. inicio=new Date(fechaInicio), fin=new Date(fechaFin). Validar fin>=inicio (400 si no). $transaction: crear periodoNomina { fechaInicio, fechaFin, creadoPorId: req.user!.id }, luego calcularDetallesPeriodo(tx, p.id, inicio, fin). Devolver período con detalles. ApiSuccess(res, periodo, 201).
    - POST /periodos/:id/recalcular (ADMIN): cargar período; si estado!=='BORRADOR' → ApiError 409. $transaction: calcularDetallesPeriodo(tx, id, period.fechaInicio, period.fechaFin). ApiSuccess.
    - PATCH /detalles/:id (ADMIN): safeParse ajusteSchema. Cargar detalle, validar periodo en BORRADOR (409 si no). Validar netoAPagar>=0. prisma.nominaDetalle.update con deducciones y netoAPagar. ApiSuccess.
    - GET /mi-historial (requireRole('ADMIN','INSTRUCTOR','RECEPCIONISTA')): prisma.nominaDetalle.findMany where { userId: req.user!.id } include { periodo: true } orderBy periodo.creadoEn desc. ApiSuccess. Solo SU propio historial (filtra por req.user!.id, no acepta userId del query).
  </action>
  <verify>
    <automated>grep -c "/periodos\|/recalcular\|/detalles\|/mi-historial\|BORRADOR" apps/api/src/routes/nomina.routes.ts</automated>
  </verify>
  <acceptance_criteria>
    - POST /periodos crea período + detalles dentro de $transaction usando calcularDetallesPeriodo
    - POST /periodos/:id/recalcular retorna 409 si estado !== BORRADOR
    - PATCH /detalles/:id solo edita si el período está en BORRADOR y rechaza netoAPagar negativo
    - GET /mi-historial filtra por req.user!.id y no acepta userId del cliente
    - `cd apps/api && npx tsc --noEmit` sin errores
  </acceptance_criteria>
  <done>CRUD de períodos con cálculo server-side, recálculo, ajuste manual de deducciones/neto y vista del empleado.</done>
</task>

<task type="auto">
  <name>Task 3: Endpoint de aprobación con Gasto en $transaction + marcar PAGADO + montar ruta</name>
  <files>apps/api/src/routes/nomina.routes.ts, apps/api/src/routes/index.ts</files>
  <read_first>
    - apps/api/src/routes/nomina.routes.ts (Task 2)
    - apps/api/src/routes/reservaciones.routes.ts (patrón $transaction con efecto contable)
    - apps/api/src/routes/index.ts
  </read_first>
  <action>
    En nomina.routes.ts agregar:

    - POST /periodos/:id/aprobar (requireRole('ADMIN')): cargar periodo con include { detalles: { include: { user: { include: { staffProfile: true } } } } }. Si no existe → 404. Si periodo.estado!=='BORRADOR' → ApiError(res,'INVALID_STATUS','Solo se pueden aprobar períodos en BORRADOR',409). $transaction: update periodoNomina estado='APROBADO'. Por cada detalle: nombre = detalle.user.staffProfile ? `${staffProfile.firstName} ${staffProfile.lastName}` : detalle.user.email (FALLBACK obligatorio). fechaLabel = `${periodo.fechaInicio.toISOString().slice(0,10)}–${periodo.fechaFin.toISOString().slice(0,10)}`. gastoId = await autoCreateGasto(tx, { monto: Number(detalle.netoAPagar), concepto: `Nómina ${fechaLabel} — ${nombre}`, fecha: periodo.fechaFin, cuentaCodigo: '602', cuentaNombre: 'Sueldos y salarios', creadoPorId: req.user!.id }). tx.nominaDetalle.update where {id: detalle.id} data { gastoId }. Devolver período actualizado. ApiSuccess.
    - POST /periodos/:id/pagar (ADMIN): cargar período; si estado!=='APROBADO' → ApiError 409. update estado='PAGADO'. ApiSuccess.

    En index.ts: agregar `import nominaRoutes from './nomina.routes';` y `router.use('/personal/nomina', nominaRoutes);`.
  </action>
  <verify>
    <automated>grep -c "INVALID_STATUS\|cuentaCodigo: '602'\|estado: 'PAGADO'\|router.use('/personal/nomina'" apps/api/src/routes/nomina.routes.ts apps/api/src/routes/index.ts</automated>
  </verify>
  <acceptance_criteria>
    - POST /periodos/:id/aprobar retorna 409 si estado !== 'BORRADOR' ANTES de iniciar la transacción
    - Aprobar crea un Gasto por detalle con cuentaCodigo '602' y actualiza NominaDetalle.gastoId en la misma $transaction
    - El concepto usa fallback user.email cuando staffProfile es null
    - POST /periodos/:id/pagar exige estado APROBADO y pasa a PAGADO
    - index.ts monta `router.use('/personal/nomina', nominaRoutes);`
    - `cd apps/api && npx tsc --noEmit` sin errores
  </acceptance_criteria>
  <done>Aprobación atómica con efecto contable en cuenta 602, doble aprobación bloqueada (409), transición a PAGADO, ruta montada.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| empleado → API /mi-historial | Empleado autenticado no debe ver datos de otros |
| admin → API /aprobar | Acción con efecto contable irreversible |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-01 | Tampering | POST /periodos/:id/aprobar | mitigate | Validar estado==='BORRADOR' antes de $transaction; 409 si no — previene gastos duplicados |
| T-23-02 | Information Disclosure | GET /mi-historial | mitigate | Filtrar por req.user!.id server-side; no aceptar userId del query |
| T-23-03 | Tampering | concepto de Gasto | mitigate | concepto construido server-side desde datos de BD; no usa input del cliente |
| T-23-04 | Elevation of Privilege | endpoints de nómina admin | mitigate | requireRole('ADMIN') por-ruta en crear/aprobar/pagar/ajustar |
</threat_model>

<verification>
- `cd apps/api && npx tsc --noEmit` compila
- Aprobar dos veces el mismo período → 409 en la segunda llamada
</verification>

<success_criteria>
SC-23-4, SC-23-5, SC-23-6 cubiertos: estados de nómina, cálculo correcto de detalles e integración contable atómica en cuenta 602.
</success_criteria>

<output>
Create `.planning/phases/23-nomina-personal/23-04-SUMMARY.md` when done
</output>
