---
phase: 23-nomina-personal
plan: 05
type: execute
wave: 5
depends_on: [23-02, 23-03, 23-04]
files_modified:
  - apps/web/src/hooks/usePersonal.ts
  - apps/web/src/app/(dashboard)/personal/page.tsx
  - apps/web/src/app/(dashboard)/personal/_components/TabPersonal.tsx
  - apps/web/src/components/layout/Sidebar.tsx
autonomous: true
requirements: [SC-23-7]
must_haves:
  truths:
    - "Existe una entrada 'Personal' en el sidebar que navega a /personal"
    - "La página /personal muestra tres pestañas: Personal, Asistencia, Nómina"
    - "El Tab Personal lista empleados y permite agregar/editar con creación inline de puesto"
  artifacts:
    - path: "apps/web/src/hooks/usePersonal.ts"
      provides: "Hooks TanStack Query para puestos, staff, asistencia y nómina"
      exports: ["usePuestos", "useStaff", "useAsistenciaSemana", "useNomina", "mutations"]
    - path: "apps/web/src/app/(dashboard)/personal/page.tsx"
      provides: "Shell de página con 3 Tabs"
      contains: "Tabs"
  key_links:
    - from: "Sidebar.tsx navItems"
      to: "/personal"
      via: "entrada Users2"
      pattern: "href: \"/personal\""
    - from: "page.tsx"
      to: "TabPersonal, TabAsistencia, TabNomina"
      via: "import de _components"
      pattern: "TabPersonal"
---

<objective>
Crear el hook usePersonal.ts (todos los hooks de la fase), la página /personal con shell de 3 tabs, el componente TabPersonal (gestión de empleados + puestos con creación inline), y la entrada del sidebar.

Purpose: Cubre SC-23-7 (vista admin gestión de personal). Crea el shell y el hook compartido que los planes 06 (TabAsistencia) y 07 (TabNomina) consumen. La página importa los tres tab-components; los planes 06/07 solo CREAN sus archivos sin editar page.tsx.
Output: /personal navegable con Tab Personal funcional.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/23-nomina-personal/23-UI-SPEC.md
@apps/web/src/app/(dashboard)/contabilidad/page.tsx
@apps/web/src/hooks/useContabilidad.ts

<interfaces>
apiClient (apps/web/src/lib/api-client.ts): apiClient.get('/path', {params}), apiClient.post, etc.
  Respuesta del backend: { success, data }. Los hooks devuelven res.data.data.
useAuth() (apps/web/src/hooks/useAuth.ts): expone user.role para ocultar salario a no-ADMIN.
shadcn instalados: Tabs, Card, CardHeader, CardContent, CardTitle, Dialog, Badge, Button, Input,
  DropdownMenu, Accordion. AlertDialog NO instalado — usar Dialog con botones de confirmación.
Sidebar navItems (Sidebar.tsx:32): array de { label, href, icon }. Iconos importados de lucide-react.
  "Instructores" usa UserCheck (línea 39). Insertar Personal después de Instructores.

Endpoints backend (de planes 02-04, prefijo /personal):
  GET/POST /puestos, PATCH/DELETE /puestos/:id
  GET/POST /staff, PATCH/DELETE /staff/:id
  POST /asistencia/hoy, GET /asistencia/mi-semana, GET /asistencia/admin, PATCH /asistencia/admin
  GET/POST /nomina/periodos, POST /nomina/periodos/:id/recalcular|aprobar|pagar,
  PATCH /nomina/detalles/:id, GET /nomina/mi-historial
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Crear hook usePersonal.ts con todos los hooks de la fase</name>
  <files>apps/web/src/hooks/usePersonal.ts</files>
  <read_first>
    - apps/web/src/hooks/useContabilidad.ts (patrón useQuery/useMutation, queryKey naming, res.data.data, invalidateQueries)
  </read_first>
  <action>
    Crear usePersonal.ts con tipos TS (Puesto, StaffProfile, AsistenciaSemana, PeriodoNomina, NominaDetalle) y los hooks:

    Queries: usePuestos (queryKey ['personal-puestos'], GET /personal/puestos), useStaff (['personal-staff'], GET /personal/staff), useAsistenciaSemana(inicio) (['personal-asistencia', inicio], GET /personal/asistencia/admin?inicio=), useNomina (['personal-nomina'], GET /personal/nomina/periodos).

    Mutations (cada una invalida su queryKey en onSuccess):
    - useCrearPuesto, useEditarPuesto, useEliminarPuesto (invalidan personal-puestos)
    - useCrearStaff, useEditarStaff, useEliminarStaff (invalidan personal-staff y personal-puestos)
    - useMarcarAsistenciaAdmin (PATCH /personal/asistencia/admin, invalida personal-asistencia)
    - useCrearPeriodo, useRecalcularPeriodo, useAjustarDetalle, useAprobarNomina, useMarcarPagado.
      useAprobarNomina en onSuccess invalida ['personal-nomina'] Y ['contabilidad-gastos'] (para refrescar la vista contable).

    Todos los queries devuelven res.data.data. Usar useQueryClient para invalidar.
  </action>
  <verify>
    <automated>grep -c "usePuestos\|useStaff\|useAsistenciaSemana\|useNomina\|useAprobarNomina\|contabilidad-gastos" apps/web/src/hooks/usePersonal.ts</automated>
  </verify>
  <acceptance_criteria>
    - usePersonal.ts exporta usePuestos, useStaff, useAsistenciaSemana, useNomina y las mutations listadas
    - useAprobarNomina invalida tanto 'personal-nomina' como 'contabilidad-gastos'
    - Todos los queries leen res.data.data
    - `cd apps/web && npx tsc --noEmit` sin errores en usePersonal.ts
  </acceptance_criteria>
  <done>Hook compartido completo; planes 06 y 07 lo consumen.</done>
</task>

<task type="auto">
  <name>Task 2: Crear shell page.tsx con 3 Tabs y componente TabPersonal</name>
  <files>apps/web/src/app/(dashboard)/personal/page.tsx, apps/web/src/app/(dashboard)/personal/_components/TabPersonal.tsx</files>
  <read_first>
    - apps/web/src/app/(dashboard)/contabilidad/page.tsx (patrón de página dashboard, header, formato moneda fmt, uso de useAuth para ocultar columnas)
    - apps/web/src/hooks/usePersonal.ts (Task 1)
    - .planning/phases/23-nomina-personal/23-UI-SPEC.md (Copywriting Contract Tab 1, Interaction Contracts Vista 1, Badges activo/inactivo)
  </read_first>
  <action>
    Crear page.tsx ("use client"): header h1 "Personal y Nómina" + subtítulo "Gestión de empleados, asistencia semanal y cálculo de nómina". `<Tabs defaultValue="personal">` con TabsList (Personal | Asistencia | Nómina) y tres TabsContent. Importar `<TabPersonal />` de ./_components/TabPersonal, y para asistencia/nomina importar `<TabAsistencia />` y `<TabNomina />` de ./_components/ (estos archivos los crean los planes 06 y 07 — declarar los imports ahora; si aún no existen al compilar este plan en aislamiento, crear stubs mínimos que rendericen un div vacío, los planes 06/07 los reemplazan completamente).

    Crear _components/TabPersonal.tsx ("use client"): usa useStaff, usePuestos, useCrearStaff, useEditarStaff, useEliminarStaff, useCrearPuesto, useEliminarPuesto, useAuth. Tabla de empleados: columnas Avatar+Nombre | Puesto | Salario semanal (ocultar si user.role!=='ADMIN') | Estado (Badge activo/inactivo según UI-SPEC) | Acciones (DropdownMenu editar/eliminar). Botones "+ Agregar empleado" y "+ Nuevo puesto". Dialog agregar/editar empleado con campos Nombre, Apellido, Teléfono, Puesto (Select con opción "+ Crear puesto nuevo" inline que muestra nombre+salarioSemanal), Fecha de ingreso (input date), Activo (checkbox). Confirmación de eliminar vía Dialog (no AlertDialog). Empty state y error state con los copys del UI-SPEC. Formato moneda con Intl.NumberFormat es-MX MXN. Loading: skeleton rows.
  </action>
  <verify>
    <automated>grep -c "Tabs\|TabPersonal\|Personal y Nómina\|defaultValue=\"personal\"" "apps/web/src/app/(dashboard)/personal/page.tsx" "apps/web/src/app/(dashboard)/personal/_components/TabPersonal.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - page.tsx renderiza Tabs con valores personal/asistencia/nomina y h1 "Personal y Nómina"
    - page.tsx importa TabPersonal, TabAsistencia, TabNomina desde ./_components/
    - TabPersonal oculta la columna Salario semanal cuando user.role !== 'ADMIN'
    - TabPersonal tiene Dialog con creación inline de puesto (opción "+ Crear puesto nuevo")
    - Badge de estado usa las clases del UI-SPEC (emerald para activo, muted para inactivo)
    - `cd apps/web && npx tsc --noEmit` sin errores
  </acceptance_criteria>
  <done>Página /personal con shell de 3 tabs y Tab Personal funcional.</done>
</task>

<task type="auto">
  <name>Task 3: Agregar entrada 'Personal' al Sidebar</name>
  <files>apps/web/src/components/layout/Sidebar.tsx</files>
  <read_first>
    - apps/web/src/components/layout/Sidebar.tsx (import block de lucide-react línea 6-22, array navItems línea 32)
  </read_first>
  <action>
    Agregar `Users2` al import de lucide-react (junto a UserCheck). En el array navItems insertar `{ label: "Personal", href: "/personal", icon: Users2 }` inmediatamente DESPUÉS de la entrada "Instructores" (línea 39). No reordenar las demás.
  </action>
  <verify>
    <automated>grep -c "Users2\|href: \"/personal\"" apps/web/src/components/layout/Sidebar.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Sidebar.tsx importa Users2 de lucide-react
    - navItems contiene `{ label: "Personal", href: "/personal", icon: Users2 }` después de Instructores
    - `cd apps/web && npx tsc --noEmit` sin errores
  </acceptance_criteria>
  <done>Entrada "Personal" visible en el sidebar navegando a /personal.</done>
</task>

</tasks>

<verification>
- `cd apps/web && npx tsc --noEmit` compila
- /personal renderiza 3 tabs; Tab Personal lista y gestiona empleados
</verification>

<success_criteria>
SC-23-7 cubierto: vista admin de gestión de personal (Tab 1) con CRUD de empleados y puestos, hook compartido y navegación.
</success_criteria>

<output>
Create `.planning/phases/23-nomina-personal/23-05-SUMMARY.md` when done
</output>
