---
phase: 23-nomina-personal
plan: 06
type: execute
wave: 6
depends_on: [23-05]
files_modified:
  - apps/web/src/app/(dashboard)/personal/_components/TabAsistencia.tsx
autonomous: true
requirements: [SC-23-8]
must_haves:
  truths:
    - "El Tab Asistencia muestra una grilla semanal de empleados × días"
    - "Marcar un checkbox guarda inmediatamente (autoguardado optimista) sin botón Guardar"
    - "El selector de semana permite navegar semana anterior/siguiente"
  artifacts:
    - path: "apps/web/src/app/(dashboard)/personal/_components/TabAsistencia.tsx"
      provides: "Checklist semanal de asistencia con autoguardado"
      contains: "TabAsistencia"
  key_links:
    - from: "TabAsistencia checkbox onChange"
      to: "useMarcarAsistenciaAdmin mutation"
      via: "PATCH /personal/asistencia/admin"
      pattern: "useMarcarAsistenciaAdmin"
---

<objective>
Crear el componente TabAsistencia.tsx: grilla semanal (empleados × 7 días) con checkboxes de autoguardado optimista y selector de semana con navegación.

Purpose: Cubre SC-23-8 (vista admin checklist asistencia semanal). Reemplaza el stub creado por el plan 05. Solo toca su propio archivo → paraleliza con el plan 07.
Output: Tab Asistencia funcional dentro de /personal.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/23-nomina-personal/23-UI-SPEC.md

<interfaces>
Hooks (apps/web/src/hooks/usePersonal.ts, del plan 05):
  useAsistenciaSemana(inicio: string) → { staff: [...], asistencias: [...] }
  useMarcarAsistenciaAdmin() → mutation({ userId, fecha, presente, observaciones? })
useAuth() → user.role, user.id (para resaltar la fila propia y restringir empleado a HOY)
date-fns disponible: startOfWeek, endOfWeek, addWeeks, format, eachDayOfInterval.
shadcn: Card, Button (variant ghost/icon para flechas), Checkbox. lucide: ChevronLeft, ChevronRight, Loader2.

UI-SPEC Vista 2: celda marcada bg-accent/60; día actual header text-primary font-semibold;
  días futuros checkbox disabled opacity-40; empleado no-admin solo HOY editable; área táctil td 44px;
  autoguardado en onChange (sin botón Guardar); Loader2 14px mientras mutation isPending.
  Toast: success "Asistencia guardada", error "No se pudo guardar la asistencia".
  Selector semana: formato "28 jun – 4 jul 2026", flechas Button ghost icon.
  Días abreviados Lun..Dom. Empty state "No hay empleados activos para mostrar."
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Crear TabAsistencia.tsx con grilla semanal y selector de semana</name>
  <files>apps/web/src/app/(dashboard)/personal/_components/TabAsistencia.tsx</files>
  <read_first>
    - apps/web/src/hooks/usePersonal.ts (firmas de useAsistenciaSemana y useMarcarAsistenciaAdmin)
    - .planning/phases/23-nomina-personal/23-UI-SPEC.md (Interaction Contracts Vista 2, Copywriting Tab 2, Color accent)
    - apps/web/src/app/(dashboard)/personal/_components/TabPersonal.tsx (estilo/convenciones del proyecto en el mismo módulo)
  </read_first>
  <action>
    Crear TabAsistencia.tsx ("use client"). Estado local semanaInicio (Date, default startOfWeek hoy con weekStartsOn:1 lunes). Selector de semana arriba a la derecha: heading de sección "Registro semanal de asistencia" + label "28 jun – 4 jul 2026" con date-fns format, flechas ChevronLeft/ChevronRight (Button variant ghost size icon) que hacen addWeeks ±1.

    useAsistenciaSemana(format(semanaInicio,'yyyy-MM-dd')) para cargar { staff, asistencias }. Construir un Map de asistencias por `${userId}_${fechaISO}` para lookup O(1).

    Grilla `<table>`: primera columna sticky con nombre del empleado (resaltar fila si staff.user.id === user.id con bg-accent). 7 columnas de día (Lun..Dom, header del día actual en text-primary font-semibold). Cada celda: Checkbox centrado 20px, td con min-height 44px. Celda marcada (presente=true): bg-accent/60. Días futuros: checkbox disabled opacity-40. Si user.role no es ADMIN/INSTRUCTOR: solo permitir editar; admin/instructor edita todo.

    onChange del checkbox: llamar useMarcarAsistenciaAdmin.mutate({ userId: staff.user.id, fecha: fechaISO, presente: nuevoValor }) inmediatamente (autoguardado). Mientras isPending mostrar Loader2 14px en esa celda. toast.success/error según resultado. Empty state si no hay staff activo.
  </action>
  <verify>
    <automated>grep -c "useAsistenciaSemana\|useMarcarAsistenciaAdmin\|bg-accent\|ChevronLeft\|Loader2" "apps/web/src/app/(dashboard)/personal/_components/TabAsistencia.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - TabAsistencia.tsx renderiza una grilla empleados × 7 días con checkboxes
    - onChange del checkbox dispara useMarcarAsistenciaAdmin sin botón "Guardar" intermedio
    - Celda con presente=true usa bg-accent/60; días futuros disabled
    - Selector de semana con ChevronLeft/ChevronRight y label de rango formateado con date-fns
    - toast.success "Asistencia guardada" y toast.error "No se pudo guardar la asistencia"
    - `cd apps/web && npx tsc --noEmit` sin errores
  </acceptance_criteria>
  <done>Tab Asistencia con checklist semanal y autoguardado optimista funcional.</done>
</task>

</tasks>

<verification>
- `cd apps/web && npx tsc --noEmit` compila
- /personal → Tab Asistencia muestra grilla y guarda al marcar
</verification>

<success_criteria>
SC-23-8 cubierto: checklist semanal de asistencia con autoguardado y navegación de semanas.
</success_criteria>

<output>
Create `.planning/phases/23-nomina-personal/23-06-SUMMARY.md` when done
</output>
