---
phase: 23-nomina-personal
plan: 07
type: execute
wave: 6
depends_on: [23-05]
files_modified:
  - apps/web/src/app/(dashboard)/personal/_components/TabNomina.tsx
autonomous: true
requirements: [SC-23-9]
must_haves:
  truths:
    - "El Tab Nómina lista períodos en Cards con badge de estado"
    - "ADMIN puede crear un período; el servidor calcula días y neto"
    - "En BORRADOR, deducciones y neto son editables inline; aprobar pide confirmación"
    - "Aprobar registra los gastos y muestra toast de éxito; PAGADO es read-only"
  artifacts:
    - path: "apps/web/src/app/(dashboard)/personal/_components/TabNomina.tsx"
      provides: "Panel de nómina con períodos, detalles editables, aprobar/pagar"
      contains: "TabNomina"
  key_links:
    - from: "TabNomina botón Aprobar"
      to: "useAprobarNomina mutation"
      via: "POST /personal/nomina/periodos/:id/aprobar"
      pattern: "useAprobarNomina"
---

<objective>
Crear TabNomina.tsx: lista de períodos en Cards (BORRADOR arriba, APROBADO/PAGADO en Accordion), tabla de NominaDetalle con deducciones/neto editables inline en BORRADOR, Dialog crear período, y acciones aprobar/marcar pagado con confirmación.

Purpose: Cubre SC-23-9 (vista admin panel de nómina). Reemplaza el stub del plan 05; solo toca su propio archivo → paraleliza con el plan 06.
Output: Tab Nómina funcional dentro de /personal.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/23-nomina-personal/23-UI-SPEC.md

<interfaces>
Hooks (apps/web/src/hooks/usePersonal.ts, plan 05):
  useNomina() → PeriodoNomina[] con detalles[].user.staffProfile.puesto
  useCrearPeriodo({ fechaInicio, fechaFin }), useRecalcularPeriodo(id),
  useAjustarDetalle({ id, deducciones, netoAPagar }), useAprobarNomina(id), useMarcarPagado(id)

UI-SPEC Vista 3:
  Períodos BORRADOR arriba (creadoEn desc); APROBADO/PAGADO en Accordion "Períodos anteriores".
  Cada período = Card: header rango fechas + Badge estado + botón acción principal.
  Tabla detalles: Empleado | Puesto | Días | Bruto | Deducciones | Neto. En BORRADOR deducciones/neto = Input number inline. APROBADO/PAGADO solo lectura text-right tabular-nums. Fila totales font-semibold bg-muted/50.
  Botones: BORRADOR→"Aprobar nómina"(default)+"Eliminar"(ghost trash); APROBADO→"Marcar como pagado"(outline); PAGADO→ninguno.
  Badges estado: BORRADOR bg-accent; APROBADO emerald; PAGADO muted (clases exactas en UI-SPEC).
  Dialog crear período: fechaInicio + fechaFin (input date), validar fin>=inicio, mensaje informativo de cálculo automático.
  Confirmaciones: usar Dialog (AlertDialog no instalado). Copys de aprobar/pagar en UI-SPEC.
  Toast aprobar éxito: "Nómina aprobada — {N} gastos registrados en contabilidad".
  Validación: neto a pagar no puede ser negativo ("Ajuste inválido...").
  Formato moneda Intl.NumberFormat es-MX MXN. Empty state y skeleton cards.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Crear TabNomina.tsx con lista de períodos, detalles editables y crear período</name>
  <files>apps/web/src/app/(dashboard)/personal/_components/TabNomina.tsx</files>
  <read_first>
    - apps/web/src/hooks/usePersonal.ts (firmas de useNomina, useCrearPeriodo, useAjustarDetalle, useAprobarNomina, useMarcarPagado, useRecalcularPeriodo)
    - .planning/phases/23-nomina-personal/23-UI-SPEC.md (Interaction Contracts Vista 3, Copywriting Tab 3, Badges EstadoNomina)
  </read_first>
  <action>
    Crear TabNomina.tsx ("use client"). useNomina() para listar. Separar períodos: estado==='BORRADOR' arriba como Cards visibles; el resto dentro de un Accordion "Períodos anteriores".

    Cada Card de período: header con rango de fechas (Intl format), Badge de estado con las clases del UI-SPEC, y botón de acción principal según estado (BORRADOR→Aprobar+Eliminar; APROBADO→Marcar como pagado; PAGADO→nada). Body: tabla de detalles con columnas Empleado | Puesto | Días | Bruto | Deducciones | Neto. En BORRADOR las celdas Deducciones y Neto son `<Input type="number">` que al cambiar llaman useAjustarDetalle({ id, deducciones, netoAPagar }); validar neto>=0 (toast.error "Ajuste inválido: el neto a pagar no puede ser negativo." si negativo). En APROBADO/PAGADO solo lectura text-right tabular-nums. Fila de totales font-semibold bg-muted/50 sumando neto.

    Botón "+ Crear período" abre Dialog "Nuevo período de nómina" con inputs date fechaInicio/fechaFin, validación fin>=inicio, mensaje informativo de cálculo automático; submit llama useCrearPeriodo.

    Aprobar: Dialog de confirmación con el copy de UI-SPEC ("¿Aprobar el período... Se registrarán {N} gastos..."); al confirmar useAprobarNomina; onSuccess toast "Nómina aprobada — {N} gastos registrados en contabilidad". Marcar pagado: Dialog confirmación + useMarcarPagado. Empty state y skeleton cards de UI-SPEC.
  </action>
  <verify>
    <automated>grep -c "useNomina\|useCrearPeriodo\|useAprobarNomina\|useAjustarDetalle\|Accordion\|tabular-nums" "apps/web/src/app/(dashboard)/personal/_components/TabNomina.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - TabNomina lista períodos BORRADOR como Cards y APROBADO/PAGADO en Accordion
    - En BORRADOR deducciones y neto son Input editables que llaman useAjustarDetalle
    - Neto negativo se rechaza con toast "Ajuste inválido: el neto a pagar no puede ser negativo."
    - Aprobar muestra Dialog de confirmación y onSuccess toast con N gastos
    - Botones por estado: BORRADOR=Aprobar+Eliminar, APROBADO=Marcar pagado, PAGADO=ninguno
    - Dialog crear período valida fechaFin >= fechaInicio
    - `cd apps/web && npx tsc --noEmit` sin errores
  </acceptance_criteria>
  <done>Tab Nómina con panel de períodos, ajuste inline, aprobación con confirmación y transición a pagado.</done>
</task>

</tasks>

<verification>
- `cd apps/web && npx tsc --noEmit` compila
- `cd apps/web && npm run build` completa (gate final de la fase)
</verification>

<success_criteria>
SC-23-9 cubierto: panel de nómina (Tab 3) con creación de período, edición inline de deducciones/neto, aprobación con efecto contable y marcado de pago.
</success_criteria>

<output>
Create `.planning/phases/23-nomina-personal/23-07-SUMMARY.md` when done
</output>
